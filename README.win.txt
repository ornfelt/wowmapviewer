#to compile stormlib, just cd into it and run 'make -f makefile.w32'

#then open wowmapviewer.dev in dev-c++ and compile normaly

#copy stormlib/StormLib.dll to bin/
#run WowMapViewer.exe in bin/

Do this:
Open src/stormlib/StormLib.sln

compile ReleaseAD or DebugAD / DebugAS?

open src/wowmapview.sln

Compile in Release / Debug